{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && pnpm --filter backend prisma migrate deploy && pnpm --filter backend prisma generate && pnpm --filter backend build"
  },
  "deploy": {
    "startCommand": "node dist/src/main",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
